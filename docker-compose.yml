services:
  haxball-host:
    build: .
    container_name: haxball-host
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
